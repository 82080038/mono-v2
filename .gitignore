# Only exclude truly sensitive production files
.env.production

# Exclude very large generated files
*.sql.gz
*.zip
*.tar.gz

# IDE/OS system files
.DS_Store
Thumbs.db

# Node dependencies
node_modules/
tests/puppeteer/node_modules/
npm-debug.log*

# Test artifacts (keep results.json but skip cache)
tests/puppeteer/.cache/
